{
  "name": "flow-jest-project",
  "version": "0.0.0",
  "private": true,
  "description": "A module to test a flow project, see https://flow.org/",
  "scripts": {
    "test:unit": "jest --testPathIgnorePatterns verify",
    "test": "stryker run",
    "posttest": "mocha --no-config --require ../../tasks/ts-node-register.js verify/*.ts"
  }
}
