{
  "name": "typescript-transpiling",
  "version": "0.0.0",
  "private": true,
  "description": "A module to perform an integration test",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "clean:reports": "rimraf \"reports\" \"dist\" \"stryker.log\"",
    "clean:dist": "rimraf dist",
    "build": "npm run clean:dist && tsc",
    "verify": "mocha --no-config --no-package --timeout 0 verify/verify.js",
    "test:unit": "npm run build && mocha",
    "test": "npm run clean:reports && npm run test:stryker && npm run test:stryker-native-preview && npm run verify",
    "test:stryker": "stryker run",
    "test:stryker-native-preview": "stryker run stryker.conf.native-preview.json"
  },
  "mocha": {
    "spec": [
      "dist/test/**/*.js"
    ]
  },
  "author": "",
  "license": "ISC"
}
