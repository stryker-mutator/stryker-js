{
  "name": "mutate-specific-lines",
  "description": "A test project for only running specific mutants, rather than all mutants",
  "scripts": {
    "test": "stryker run -m src/index.js:3:18:3:26,src/index.js:4:18:4:25",
    "test:unit": "jest",
    "posttest": "mocha --require ../../tasks/ts-node-register.js verify/verify.ts"
  },
  "devDependencies": {
    "@types/jest": "~25.2.1",
    "@types/node": "^14.0.27"
  }
}
