import {StrykerOptions} from '../core';

/**
 * Represents an settings object for a TestSelector.
 */
interface TestSelectorSettings {
  /**
   * The StrykerOptions.
   */
  options: StrykerOptions;
}

export default TestSelectorSettings;