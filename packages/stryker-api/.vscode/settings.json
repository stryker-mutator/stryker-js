{
  "typescript.tsdk": "../../node_modules/typescript/lib",
  "files.exclude": {
    ".git": true,
    "**/*.js": {
      "when": "$(basename).ts"
    },
    "**/*.d.ts": {
      "when": "$(basename).ts"
    },
    "**/*.map": {
      "when": "$(basename)"
    }
  }
}