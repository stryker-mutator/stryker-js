import { expect } from 'chai';

import { Instrumenter } from '../../src';

describe(Instrumenter.name, () => {
  it('remove this test in the future', () => {
    expect(new Instrumenter()).instanceOf(Instrumenter);
  });
});
