#!/usr/bin/env node

'use strict';

process.title = 'stryker';
console.log(`CLI PID: ${process.pid}`);

// Run the Stryker CLI
const StrykerCli = require('../src/StrykerCli').default;
new StrykerCli(process.argv).run();
