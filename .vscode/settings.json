{
  "typescript.tsdk": "node_modules/typescript/lib",
  "files.exclude": {

    ".git": true,
    ".tscache": true,
    "**/*.js": {
      "when": "$(basename).ts"
    },
    "**/*.map": {
      "when": "$(basename)"
    }
  }
}
