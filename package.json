{
  "private": true,
  "devDependencies": {
    "lerna": "^2.0.0-rc.0",
    "typescript": "^2.2.1"
  },
  "scripts": {
    "postinstall": "lerna bootstrap",
    "build": "lerna run build",
    "pretest": "npm run build",
    "test": "lerna exec npm test"
  }
}
